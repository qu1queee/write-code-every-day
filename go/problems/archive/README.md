# Archive

Pre-curriculum solutions migrated from the original `golang/src/` layout.

These are kept for reference and katacoding review. New work follows the phase-based structure in `go/problems/phase*/`.

Topic folders:
- `arrays_and_strings/`
- `binary_tree/`
- `linked_list/`
- plus standalone problems at the root (`isomorphic/`, `move_zeroes/`, etc.)
