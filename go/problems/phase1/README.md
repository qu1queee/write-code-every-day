# Phase 1 — Linear Structures

Add new problems here using the naming convention `lc<NNNN>_<slug>/`.

Each folder should contain:
- `solution.go` — current best solution
- `solution_test.go` — table-driven tests
- `notes.md` (optional) — pattern notes, complexity, katacoding log

See `plans/datastructures_and_algorithms.md` for the problem list.
