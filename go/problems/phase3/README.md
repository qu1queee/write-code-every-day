# Phase 3 — Graphs, Tries, Design Problems

Add new problems here using the naming convention `lc<NNNN>_<slug>/`.

Before solving design problems, write in `notes.md` what data structure backs each method and why.

See `plans/datastructures_and_algorithms.md` for the problem list.
